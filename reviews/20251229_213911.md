# RG Family - Minimal & Refined Hip 디자인 구현

**Date:** 2025-12-29 21:39:11
**User Request:** next 1.2.3 - 조직도 트리, VIP 헌정 페이지, Hero 배너 멤버 이미지

---

## Changes Summary

| Type | File | Reason |
|------|------|--------|
| MODIFICATION | `src/app/info/org/page.tsx` | 조직도 트리 구조 변경 - 계층별 연결선 시각화 |
| MODIFICATION | `src/app/info/org/page.module.css` | 트리 연결선 CSS 추가 (수직/수평 핑크 그라디언트) |
| MODIFICATION | `src/app/ranking/vip/[userId]/page.tsx` | 랭크 기반 동적 테마를 위한 data-rank 속성 추가 |
| MODIFICATION | `src/app/ranking/vip/[userId]/page.module.css` | Gold/Silver/Bronze 동적 테마 CSS 변수 |
| MODIFICATION | `src/lib/mock/data.ts` | dicebear API로 플레이스홀더 멤버 이미지 생성 |
| MODIFICATION | `src/components/Hero.module.css` | 멤버 이미지 플로팅 애니메이션, 글로우 효과, 반응형 |

---

## Detailed Changes

### 1. src/app/info/org/page.tsx

**Type:** MODIFICATION
**Reason:** 조직도 트리 구조 변경 - 계층별 연결선 시각화

**Before:**
```
기존 그리드 레이아웃
```

**After:**
```
orgTree, treeLevel, treeLine, treeNodes 구조
```

### 2. src/app/info/org/page.module.css

**Type:** MODIFICATION
**Reason:** 트리 연결선 CSS 추가 (수직/수평 핑크 그라디언트)

**Before:**
```
기존 membersGrid 스타일
```

**After:**
```
.orgTree { display: flex; flex-direction: column; }
.treeLine { height: 40px; }
.verticalLine { background: linear-gradient(180deg, var(--color-primary)...) }
.horizontalConnector { background: linear-gradient(90deg, transparent...) }
.nodeConnector { width: 2px; height: 24px; }
```

### 3. src/app/ranking/vip/[userId]/page.tsx

**Type:** MODIFICATION
**Reason:** 랭크 기반 동적 테마를 위한 data-rank 속성 추가

**Before:**
```
기존 main className
```

**After:**
```
const rankForTheme = data?.reward?.rank <= 3 ? data.reward.rank : 0
<main data-rank={rankForTheme}>
```

### 4. src/app/ranking/vip/[userId]/page.module.css

**Type:** MODIFICATION
**Reason:** Gold/Silver/Bronze 동적 테마 CSS 변수

**Before:**
```
기존 고정 색상
```

**After:**
```
.main { --rank-color: #fd68ba; --rank-gradient: linear-gradient(...) }
.main[data-rank="1"] { --rank-color: #ffd700; } /* Gold */
.main[data-rank="2"] { --rank-color: #c0c0c0; } /* Silver */
.main[data-rank="3"] { --rank-color: #cd7f32; } /* Bronze */
.avatar { border: 4px solid var(--rank-color); }...
```

### 5. src/lib/mock/data.ts

**Type:** MODIFICATION
**Reason:** dicebear API로 플레이스홀더 멤버 이미지 생성

**Before:**
```
memberImages: ['/assets/members/nano.jpg', ...]
```

**After:**
```
const getMemberCharacterImage = (seed) =>
  `https://api.dicebear.com/7.x/lorelei/svg?seed=${seed}...`
memberImages: [getMemberCharacterImage('nano-rg'), getMemberCharacterImage('banana-rg')]
```

### 6. src/components/Hero.module.css

**Type:** MODIFICATION
**Reason:** 멤버 이미지 플로팅 애니메이션, 글로우 효과, 반응형

**Before:**
```
기존 characterContainer 스타일
```

**After:**
```
.characterContainer {
  filter: drop-shadow(0 0 60px rgba(253, 104, 186, 0.3));
  animation: characterFloat 6s ease-in-out infinite;
}
@keyframes characterFloat { 0%,100% { translateY(0) } 50% { translateY(-10px) } }
.characterContainer::before { /* glow effect */ }
.characterContainer:only-child { ...
```

