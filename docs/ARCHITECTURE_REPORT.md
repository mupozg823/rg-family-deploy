# RG Family - 프로젝트 아키텍처 종합 분석 보고서

> **분석 일시**: 2026-01-23
> **브랜치**: feature/season-donation-rankings
> **분석 도구**: Claude Code + Explore Agent

---

## 목차

1. [Executive Summary](#1-executive-summary)
2. [프로젝트 스캐폴딩](#2-프로젝트-스캐폴딩)
3. [아키텍처 다이어그램](#3-아키텍처-다이어그램)
4. [핵심 데이터 플로우](#4-핵심-데이터-플로우)
5. [API 엔드포인트 분석](#5-api-엔드포인트-분석)
6. [코드 품질 분석](#6-코드-품질-분석)
7. [발견된 문제점 및 개선 사항](#7-발견된-문제점-및-개선-사항)
8. [상업화 로드맵](#8-상업화-로드맵)
9. [결론 및 권장 사항](#9-결론-및-권장-사항)

---

## 1. Executive Summary

### 1.1 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | RG Family |
| **목적** | PandaTV 스트리머 "리나" 팬 커뮤니티 공식 웹사이트 |
| **대상** | 내부 팬 + VIP 후원자 |
| **플랫폼** | PandaTV (후원 단위: 하트) |

### 1.2 기술 스택 요약

```
┌─────────────────────────────────────────────────────────────┐
│                     Frontend                                 │
│  Next.js 16+ │ React 19 │ Tailwind 4 │ shadcn/ui │ Mantine │
├─────────────────────────────────────────────────────────────┤
│                     State Management                         │
│        React Context │ TanStack Query │ Zustand             │
├─────────────────────────────────────────────────────────────┤
│                     Backend (BaaS)                           │
│     Supabase (PostgreSQL) │ Supabase Auth │ Cloudinary      │
├─────────────────────────────────────────────────────────────┤
│                     Deployment                               │
│               Vercel │ Vercel Cron Jobs                      │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 핵심 수치

| 메트릭 | 수치 |
|--------|------|
| 총 테이블 수 | 30+ |
| Server Actions | 21개 파일 |
| Custom Hooks | 27개 |
| React 컴포넌트 | 200+ |
| 관리자 페이지 | 25개 |
| API 엔드포인트 | 4개 |
| Repository 인터페이스 | 20+ |

---

## 2. 프로젝트 스캐폴딩

### 2.1 디렉토리 구조

```
rg-family/
├── 📁 src/
│   ├── 📁 app/                    # Next.js App Router (페이지)
│   │   ├── 📁 (auth)/            # 인증 페이지 그룹
│   │   ├── 📁 admin/             # 관리자 페이지 (25개)
│   │   │   ├── analytics/        # 분석 대시보드
│   │   │   ├── banners/          # 배너 관리
│   │   │   ├── donations/        # 후원 데이터
│   │   │   ├── episodes/         # 에피소드/직급전
│   │   │   ├── members/          # 회원 관리
│   │   │   ├── organization/     # 조직도
│   │   │   ├── permissions/      # 권한 관리
│   │   │   ├── schedules/        # 일정 관리
│   │   │   ├── seasons/          # 시즌 관리
│   │   │   ├── signatures/       # 시그니처
│   │   │   ├── timeline/         # 타임라인
│   │   │   └── vip-rewards/      # VIP 리워드
│   │   ├── 📁 api/               # REST API (4개)
│   │   │   ├── live-status/      # 라이브 상태 동기화
│   │   │   ├── panda-notice/     # PandaTV 공지
│   │   │   └── upload/           # 파일 업로드
│   │   ├── 📁 community/         # 커뮤니티 (자유/VIP)
│   │   ├── 📁 ranking/           # 랭킹 시스템
│   │   │   ├── hall-of-fame/     # 명예의 전당
│   │   │   ├── season/           # 시즌별 랭킹
│   │   │   ├── tribute/          # 헌정 페이지
│   │   │   └── vip/              # VIP 프로필
│   │   ├── 📁 rg/                # RG 정보
│   │   │   ├── history/          # 히스토리
│   │   │   ├── live/             # 생방송
│   │   │   ├── org/              # 조직도
│   │   │   └── sig/              # 시그니처
│   │   ├── 📁 schedule/          # 일정 캘린더
│   │   └── 📄 globals.css        # CSS 변수/테마
│   │
│   ├── 📁 components/            # React 컴포넌트 (200+)
│   │   ├── 📁 admin/             # 관리자 UI (50+)
│   │   │   ├── analytics/        # 분석 차트
│   │   │   ├── DataTable.tsx     # 공용 데이터 테이블
│   │   │   └── Sidebar.tsx       # 사이드바
│   │   ├── 📁 common/            # 공용 컴포넌트
│   │   ├── 📁 home/              # 홈페이지
│   │   ├── 📁 ranking/           # 랭킹 UI (25+)
│   │   ├── 📁 schedule/          # 캘린더 (8+)
│   │   │   ├── AdminScheduleOverlay.tsx  # 관리자 CRUD
│   │   │   ├── Calendar.tsx
│   │   │   ├── CalendarGrid.tsx
│   │   │   ├── EventDetailModal.tsx
│   │   │   └── ScheduleEditModal.tsx
│   │   ├── 📁 tribute/           # 헌정 (15+)
│   │   ├── 📁 ui/                # shadcn/ui (20+)
│   │   └── 📁 vip/               # VIP 시스템 (30+)
│   │
│   ├── 📁 lib/                   # 비즈니스 로직
│   │   ├── 📁 actions/           # Server Actions (21개)
│   │   │   ├── index.ts          # 래퍼 (adminAction, authAction, publicAction)
│   │   │   ├── donations.ts      # 후원 CRUD
│   │   │   ├── seasons.ts        # 시즌 CRUD
│   │   │   ├── episodes.ts       # 에피소드 CRUD
│   │   │   ├── hall-of-fame.ts   # 명예의 전당
│   │   │   ├── schedules.ts      # 일정 CRUD
│   │   │   ├── vip-messages.ts   # VIP 메시지
│   │   │   └── analytics.ts      # 분석 데이터
│   │   ├── 📁 api/               # API 통합 모듈
│   │   │   └── pandatv/          # PandaTV 통합
│   │   ├── 📁 context/           # React Context (5개)
│   │   │   ├── AuthContext.tsx
│   │   │   ├── DataProviderContext.tsx
│   │   │   ├── SupabaseContext.tsx
│   │   │   └── ThemeContext.tsx
│   │   ├── 📁 hooks/             # Custom Hooks (27개)
│   │   │   ├── useRanking.ts
│   │   │   ├── useSchedule.ts
│   │   │   ├── useVipStatus.ts
│   │   │   └── useAnalytics.ts
│   │   ├── 📁 repositories/      # Repository 패턴
│   │   │   ├── types.ts          # 인터페이스 정의 (20+)
│   │   │   ├── 📁 supabase/      # Supabase 구현체
│   │   │   └── 📁 mock/          # Mock 구현체 (사용 금지)
│   │   ├── 📁 supabase/          # Supabase 클라이언트
│   │   └── 📁 utils/             # 유틸리티 (14개)
│   │
│   └── 📁 types/                 # 타입 정의
│       ├── database.ts           # Supabase 스키마 (1900+ lines)
│       ├── common.ts
│       └── ranking.ts
│
├── 📁 scripts/                   # 데이터 마이그레이션 스크립트
├── 📄 CLAUDE.md                  # AI 개발 가이드
├── 📄 package.json
└── 📄 tsconfig.json
```

### 2.2 주요 파일 역할

| 파일 | 역할 | 라인 수 |
|------|------|--------|
| `src/types/database.ts` | Supabase 자동 생성 타입 | 1,900+ |
| `src/lib/actions/index.ts` | Server Action 래퍼 | ~100 |
| `src/lib/context/DataProviderContext.tsx` | 데이터 제공자 | ~200 |
| `src/lib/repositories/types.ts` | Repository 인터페이스 | ~500 |
| `src/app/globals.css` | CSS 변수/테마 | ~400 |

---

## 3. 아키텍처 다이어그램

### 3.1 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              CLIENT (Browser)                            │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                    Next.js App Router                            │    │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐    │    │
│  │  │   Pages   │  │Components │  │  Hooks    │  │  Context  │    │    │
│  │  │ (app/)    │  │           │  │           │  │           │    │    │
│  │  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘    │    │
│  │        │              │              │              │           │    │
│  │        └──────────────┴──────────────┴──────────────┘           │    │
│  │                              │                                   │    │
│  │                    ┌─────────▼─────────┐                        │    │
│  │                    │  Server Actions   │                        │    │
│  │                    │ (adminAction,     │                        │    │
│  │                    │  authAction,      │                        │    │
│  │                    │  publicAction)    │                        │    │
│  │                    └─────────┬─────────┘                        │    │
│  └──────────────────────────────┼──────────────────────────────────┘    │
└──────────────────────────────────┼──────────────────────────────────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
            ┌───────▼───────┐ ┌───▼───┐ ┌───────▼───────┐
            │   Supabase    │ │  API  │ │  Cloudinary   │
            │  (PostgreSQL) │ │Routes │ │   (Images)    │
            │               │ │       │ │               │
            │  ┌─────────┐  │ │ /api/ │ │  rg-family/   │
            │  │30+ Tables│  │ │upload │ │  banners/     │
            │  │10+ RPC   │  │ │live   │ │  avatars/     │
            │  │RLS Policy│  │ │notice │ │               │
            │  └─────────┘  │ └───────┘ └───────────────┘
            └───────────────┘
                    │
            ┌───────▼───────┐
            │   PandaTV     │
            │   (External)  │
            │               │
            │  - Live API   │
            │  - Notice     │
            │  - Scraper    │
            └───────────────┘
```

### 3.2 데이터 계층 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                       Presentation Layer                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Pages     │  │  Components │  │   Hooks     │             │
│  │             │  │             │  │             │             │
│  │ /ranking    │  │ RankingList │  │ useRanking  │             │
│  │ /schedule   │  │ Calendar    │  │ useSchedule │             │
│  │ /admin/*    │  │ DataTable   │  │ useAdminCRUD│             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
│         │                │                │                     │
│         └────────────────┴────────────────┘                     │
│                          │                                       │
├──────────────────────────┼───────────────────────────────────────┤
│                    Application Layer                             │
│                          │                                       │
│              ┌───────────▼───────────┐                          │
│              │    Server Actions     │                          │
│              │                       │                          │
│              │  adminAction<T>()     │──▶ Admin 권한 검증       │
│              │  authAction<T>()      │──▶ 인증 검증             │
│              │  publicAction<T>()    │──▶ 공개 접근             │
│              └───────────┬───────────┘                          │
│                          │                                       │
├──────────────────────────┼───────────────────────────────────────┤
│                     Domain Layer                                 │
│                          │                                       │
│              ┌───────────▼───────────┐                          │
│              │    Repository Pattern │                          │
│              │                       │                          │
│              │  IDataProvider        │                          │
│              │  ├─ IRankingRepository│                          │
│              │  ├─ ISeasonRepository │                          │
│              │  ├─ IDonationRepository│                         │
│              │  └─ ... (20+ interfaces)│                        │
│              └───────────┬───────────┘                          │
│                          │                                       │
├──────────────────────────┼───────────────────────────────────────┤
│                  Infrastructure Layer                            │
│                          │                                       │
│              ┌───────────▼───────────┐                          │
│              │   Supabase Client     │                          │
│              │                       │                          │
│              │  createServerClient() │                          │
│              │  createBrowserClient()│                          │
│              └───────────┬───────────┘                          │
│                          │                                       │
│              ┌───────────▼───────────┐                          │
│              │   PostgreSQL (RLS)    │                          │
│              │                       │                          │
│              │  30+ Tables           │                          │
│              │  10+ RPC Functions    │                          │
│              │  Row Level Security   │                          │
│              └───────────────────────┘                          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 권한 관리 구조

```
┌─────────────────────────────────────────────────────────────┐
│                     Role Hierarchy                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│     superadmin ──────────────────────────┐                  │
│         │                                │                  │
│         ▼                                │                  │
│       admin ─────────────────────────────┤ ADMIN_ROLES     │
│         │                                │                  │
│         ▼                                │                  │
│     moderator ───────────────────────────┤ MODERATOR_ROLES │
│         │                                │                  │
│         ▼                                │                  │
│        vip ──────────────────────────────┤                  │
│         │                                │                  │
│         ▼                                │                  │
│      member ─────────────────────────────┘                  │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│                   Access Control                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  adminAction()     → admin, superadmin                      │
│  authAction()      → 로그인 사용자                           │
│  publicAction()    → 모든 사용자                             │
│                                                              │
│  /admin/*          → admin, superadmin                      │
│  /community/vip    → vip+                                   │
│  /ranking/vip/*    → vip+                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 핵심 데이터 플로우

### 4.1 후원 랭킹 조회 플로우

```
┌──────────────────────────────────────────────────────────────────┐
│                    후원 랭킹 조회 플로우                          │
└──────────────────────────────────────────────────────────────────┘

    ┌─────────┐
    │  User   │
    └────┬────┘
         │ 1. /ranking/season/1 접속
         ▼
    ┌─────────────────┐
    │  RankingPage    │
    │  (page.tsx)     │
    └────────┬────────┘
             │ 2. useRanking(seasonId, filter)
             ▼
    ┌─────────────────┐
    │   useRanking    │
    │   (hook)        │
    └────────┬────────┘
             │ 3. fetchRankings()
             ▼
    ┌─────────────────┐
    │ getRankings()   │
    │ (server action) │
    └────────┬────────┘
             │ 4. publicAction()
             ▼
    ┌─────────────────┐
    │  Supabase RPC   │
    │                 │
    │ season_donation │
    │   _rankings     │
    │      OR         │
    │ rank_battle     │
    │   _records      │
    └────────┬────────┘
             │ 5. Return (rank, donor_name, ...)
             ▼                    ⚠️ total_amount 제외!
    ┌─────────────────┐
    │  RankingList    │
    │  Component      │
    │                 │
    │  - 순위 표시    │
    │  - 게이지 (%)   │
    │  - 닉네임       │
    └─────────────────┘
```

### 4.2 관리자 CRUD 플로우

```
┌──────────────────────────────────────────────────────────────────┐
│                    관리자 CRUD 플로우                             │
└──────────────────────────────────────────────────────────────────┘

    ┌─────────┐
    │  Admin  │
    └────┬────┘
         │ 1. /admin/schedules 접속
         ▼
    ┌─────────────────┐
    │  AdminPage      │
    │  + useAdminCRUD │
    └────────┬────────┘
             │ 2. Create/Update/Delete 요청
             ▼
    ┌─────────────────┐
    │ createSchedule  │
    │ updateSchedule  │
    │ deleteSchedule  │
    │ (server action) │
    └────────┬────────┘
             │ 3. adminAction() 래퍼
             ▼
    ┌─────────────────────────────────────────┐
    │            권한 검증                     │
    │                                          │
    │  const { data: { user } } = await       │
    │    supabase.auth.getUser()              │
    │                                          │
    │  const { data: profile } = await        │
    │    supabase.from('profiles')            │
    │    .select('role')                      │
    │    .eq('id', user.id)                   │
    │    .single()                            │
    │                                          │
    │  if (!ADMIN_ROLES.includes(role))       │
    │    throw 'Unauthorized'                 │
    └────────────────┬────────────────────────┘
                     │ 4. 권한 통과
                     ▼
    ┌─────────────────┐
    │   Supabase      │
    │                 │
    │ .insert() /     │
    │ .update() /     │
    │ .delete()       │
    └────────┬────────┘
             │ 5. revalidatePath()
             ▼
    ┌─────────────────┐
    │  캐시 무효화    │
    │                 │
    │ /admin/schedules│
    │ /schedule       │
    └─────────────────┘
```

### 4.3 라이브 상태 동기화 플로우

```
┌──────────────────────────────────────────────────────────────────┐
│                   라이브 상태 동기화 플로우                       │
└──────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │  Vercel Cron    │
    │  (매 10분)      │
    └────────┬────────┘
             │ 1. POST /api/live-status/sync
             ▼
    ┌─────────────────┐
    │  API Route      │
    │  (sync/route.ts)│
    └────────┬────────┘
             │ 2. organization 테이블에서
             │    활성 BJ 멤버 조회
             ▼
    ┌─────────────────────────────────────────┐
    │           PandaTV 통합 모듈             │
    │                                          │
    │  try {                                   │
    │    // 1차: 공식 API 시도                │
    │    result = await pandaTvApi.check()    │
    │  } catch {                              │
    │    // 2차: 스크래퍼로 fallback          │
    │    result = await scraper.check()       │
    │  }                                       │
    └────────────────┬────────────────────────┘
                     │ 3. 동시 3개씩 처리
                     ▼
    ┌─────────────────┐
    │   live_status   │
    │   테이블 upsert │
    │                 │
    │  - channel_id   │
    │  - is_live      │
    │  - viewer_count │
    │  - updated_at   │
    └────────┬────────┘
             │ 4. organization.is_live 업데이트
             ▼
    ┌─────────────────┐
    │ 홈페이지 반영   │
    │                 │
    │ 라이브 멤버     │
    │ 시안색(#00d4ff) │
    │ 테두리 표시     │
    └─────────────────┘
```

---

## 5. API 엔드포인트 분석

### 5.1 API 목록

| 엔드포인트 | 메소드 | 인증 | 캐싱 | 용도 |
|------------|--------|------|------|------|
| `/api/upload` | POST | Supabase Auth | - | Cloudinary 이미지 업로드 |
| `/api/live-status/sync` | POST | Cron Secret | - | 라이브 상태 동기화 |
| `/api/live-status/update` | POST | - | - | 단일 라이브 업데이트 |
| `/api/panda-notice` | GET | - | 10분 ISR | PandaTV 공지 조회 |

### 5.2 업로드 API 상세

```typescript
// POST /api/upload
{
  file: FormData,
  subfolder?: 'banners' | 'avatars' | 'signatures' | ...
}

// Response
{
  url: string,           // Cloudinary URL
  public_id: string,     // 파일 식별자
  width: number,
  height: number
}

// 자동 리사이징:
// - banners: 1500x350
// - 기타: 400x400
// - GIF: 원본 유지
```

---

## 6. 코드 품질 분석

### 6.1 발견된 문제점

#### 🔴 Critical (즉시 수정 필요)

| # | 문제 | 파일 | 영향도 |
|---|------|------|--------|
| 1 | 순환 참조 | `context/index.ts` ↔ `hooks/useEpisodes.ts` | 빌드 경고 |

**상세:**
```typescript
// src/lib/context/index.ts (라인 34)
export { useEpisodes } from '@/lib/hooks/useEpisodes'  // ❌

// src/lib/hooks/useEpisodes.ts
import { useSupabaseContext } from '@/lib/context'     // ❌ 순환
```

**수정 방법:**
```typescript
// context/index.ts에서 해당 라인 제거
// hooks/index.ts에서 export
```

#### 🟡 Warning (개선 권장)

| # | 문제 | 파일 | 영향도 |
|---|------|------|--------|
| 2 | 중복 utils 진입점 | `lib/utils.ts` & `lib/utils/index.ts` | 혼란 |
| 3 | 미사용 Mock 유틸 | `lib/utils/mock.ts` | 불필요 코드 |
| 4 | CRUD 패턴 반복 | 21개 actions 파일 | 유지보수 |
| 5 | Modal 컴포넌트 중복 | 5개 Modal 파일 | 코드 중복 |

### 6.2 코드 메트릭

```
┌────────────────────────────────────────────────────────────┐
│                    코드 품질 점수                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  아키텍처 설계      ████████████████████░░  85/100        │
│  코드 재사용성      ████████████████░░░░░░  70/100        │
│  타입 안전성        ████████████████████░░  90/100        │
│  테스트 커버리지    ████░░░░░░░░░░░░░░░░░░  20/100        │
│  문서화            ████████████░░░░░░░░░░  55/100        │
│  보안              ████████████████████░░  85/100        │
│                                                            │
│  종합 점수         ████████████████░░░░░░  68/100        │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 6.3 아키텍처 평가

| 영역 | 평가 | 설명 |
|------|------|------|
| **Clean Architecture** | ✅ 우수 | Repository 패턴으로 데이터 접근 추상화 |
| **Server Actions** | ✅ 우수 | 3가지 래퍼로 권한 관리 일관성 |
| **Type Safety** | ✅ 우수 | Supabase 타입 자동 생성 활용 |
| **State Management** | ⚠️ 양호 | Context 과다 사용 경향 |
| **Error Handling** | ⚠️ 양호 | 일부 누락된 에러 처리 |
| **Testing** | ❌ 미흡 | E2E 테스트만 존재, 단위 테스트 부재 |

---

## 7. 발견된 문제점 및 개선 사항

### 7.1 즉시 해결 (Priority: Critical)

#### Issue #1: 순환 참조 제거

```diff
// src/lib/context/index.ts
- export { useEpisodes } from '@/lib/hooks/useEpisodes'

// src/lib/hooks/index.ts
+ export { useEpisodes } from './useEpisodes'
```

#### Issue #2: Utils 경로 통일

```diff
// src/lib/utils.ts 삭제
- export * from './utils/cn';
- export * from './utils/format';
- ...

// src/lib/utils/index.ts 유지 (단일 진입점)
export * from './cn'
export * from './format'
export * from './ranking'
...
```

### 7.2 단기 개선 (1-2주)

#### Issue #3: CRUD 템플릿 추출

```typescript
// src/lib/actions/crud-factory.ts (신규)
export function createCrudActions<T>(
  tableName: string,
  revalidatePaths: string[]
) {
  return {
    create: async (data) => adminAction(async (supabase) => {
      const { data: result, error } = await supabase
        .from(tableName).insert(data).select().single()
      if (error) throw error
      return result
    }, revalidatePaths),

    update: async (id, data) => adminAction(async (supabase) => {
      // ...
    }, revalidatePaths),

    delete: async (id) => adminAction(async (supabase) => {
      // ...
    }, revalidatePaths),
  }
}

// 사용 예시
const scheduleActions = createCrudActions<Schedule>(
  'schedules',
  ['/admin/schedules', '/schedule']
)
```

#### Issue #4: 공통 Modal 컴포넌트

```typescript
// src/components/common/BaseModal.tsx (신규)
interface BaseModalProps {
  title: string
  isOpen: boolean
  onClose: () => void
  children: React.ReactNode
  footer?: React.ReactNode
}

export function BaseModal({ title, isOpen, onClose, children, footer }: BaseModalProps) {
  if (!isOpen) return null

  return (
    <motion.div className={styles.overlay} onClick={onClose}>
      <motion.div className={styles.modal} onClick={(e) => e.stopPropagation()}>
        <div className={styles.header}>
          <h2>{title}</h2>
          <button onClick={onClose}><X /></button>
        </div>
        <div className={styles.content}>{children}</div>
        {footer && <div className={styles.footer}>{footer}</div>}
      </motion.div>
    </motion.div>
  )
}
```

### 7.3 중기 개선 (1-2개월)

| 항목 | 설명 | 예상 공수 |
|------|------|----------|
| 단위 테스트 추가 | Server Actions, Hooks 테스트 | 2주 |
| API 문서화 | OpenAPI Spec 생성 | 1주 |
| 에러 모니터링 | Sentry 통합 | 3일 |
| 성능 모니터링 | Vercel Analytics 강화 | 2일 |
| CI/CD 강화 | 테스트 자동화, Lint 강화 | 1주 |

---

## 8. 상업화 로드맵

### 8.1 현재 상태 평가

```
┌─────────────────────────────────────────────────────────────────┐
│                    상업화 준비 상태                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  핵심 기능 완성도    ████████████████████░░░░  80%              │
│  UI/UX 완성도       ████████████████████░░░░  80%              │
│  보안               ████████████████████░░░░  85%              │
│  성능 최적화        ████████████████░░░░░░░░  65%              │
│  테스트             ████████░░░░░░░░░░░░░░░░  30%              │
│  문서화             ████████████░░░░░░░░░░░░  50%              │
│  운영 도구          ████████████████░░░░░░░░  70%              │
│                                                                  │
│  종합 준비도        ████████████████░░░░░░░░  66%              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 Phase 1: 안정화 (1-2개월)

#### 목표: 프로덕션 품질 달성

| 항목 | 작업 | 우선순위 |
|------|------|----------|
| 버그 수정 | 순환 참조, 중복 코드 정리 | 🔴 Critical |
| 테스트 | Server Actions 단위 테스트 | 🔴 Critical |
| 모니터링 | Sentry, Vercel Analytics | 🟡 High |
| 문서화 | API 문서, 운영 가이드 | 🟡 High |
| 성능 | 이미지 최적화, 쿼리 튜닝 | 🟡 High |

#### 구체적 작업 목록

```markdown
## Week 1-2: 코드 품질
- [ ] 순환 참조 제거
- [ ] Utils 경로 통일
- [ ] CRUD 템플릿 추출
- [ ] 공통 Modal 컴포넌트 생성
- [ ] ESLint 규칙 강화

## Week 3-4: 테스트
- [ ] Server Actions 테스트 (Jest)
- [ ] Hooks 테스트 (React Testing Library)
- [ ] E2E 테스트 확장 (Playwright)
- [ ] 테스트 커버리지 50% 달성

## Week 5-6: 운영 도구
- [ ] Sentry 에러 모니터링
- [ ] Vercel Analytics 대시보드
- [ ] 로그 수집 (Supabase Logs)
- [ ] 알림 설정 (Slack/Discord)

## Week 7-8: 문서화
- [ ] API 문서 (Swagger/OpenAPI)
- [ ] 개발자 가이드
- [ ] 운영 매뉴얼
- [ ] 트러블슈팅 가이드
```

### 8.3 Phase 2: 기능 확장 (2-4개월)

#### 목표: 사용자 경험 향상

| 기능 | 설명 | 복잡도 |
|------|------|--------|
| 실시간 알림 | 후원/댓글 알림 (Supabase Realtime) | 중 |
| 푸시 알림 | 방송 시작 알림 (FCM) | 중 |
| 다국어 지원 | i18n (한국어, 영어) | 하 |
| PWA | 오프라인 지원, 앱 설치 | 중 |
| 검색 | 전문 검색 (Full-text Search) | 중 |

#### 새로운 기능 제안

```
┌─────────────────────────────────────────────────────────────────┐
│                    Phase 2 기능 로드맵                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Month 1: 실시간 기능                                           │
│  ├─ Supabase Realtime 채널 구성                                │
│  ├─ 실시간 후원 알림                                            │
│  ├─ 실시간 댓글 알림                                            │
│  └─ 라이브 뱃지 실시간 업데이트                                 │
│                                                                  │
│  Month 2: 모바일 경험                                           │
│  ├─ PWA Manifest 설정                                          │
│  ├─ Service Worker 캐싱                                        │
│  ├─ 오프라인 페이지                                             │
│  └─ 앱 스토어 없이 설치 가능                                    │
│                                                                  │
│  Month 3-4: 검색 & 분석                                         │
│  ├─ PostgreSQL Full-text Search                                 │
│  ├─ 검색 결과 하이라이트                                        │
│  ├─ 인기 검색어                                                 │
│  └─ 사용자 행동 분석                                            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.4 Phase 3: 수익화 (4-6개월)

#### 목표: 지속 가능한 비즈니스 모델

| 수익 모델 | 설명 | 가능성 |
|-----------|------|--------|
| VIP 멤버십 | 월정액 프리미엄 기능 | ⭐⭐⭐ |
| 광고 수익 | 배너/네이티브 광고 | ⭐⭐ |
| 굿즈 연계 | 굿즈 판매 수수료 | ⭐⭐ |
| 이벤트 티켓 | 팬미팅/이벤트 | ⭐⭐⭐ |

#### VIP 멤버십 기능

```markdown
## 무료 회원
- 기본 랭킹 조회
- 공개 게시판
- 일정 조회

## VIP 멤버십 (₩9,900/월)
- 전용 VIP 게시판
- BJ 개인 메시지 접근
- 프리미엄 프로필 뱃지
- 광고 없음
- 이벤트 우선 참여권
```

### 8.5 Phase 4: 확장 (6-12개월)

#### 목표: 플랫폼화

| 확장 방향 | 설명 |
|-----------|------|
| 멀티 BJ 지원 | 여러 스트리머 팬카페 호스팅 |
| API 공개 | 서드파티 앱 개발 지원 |
| 화이트라벨 | 맞춤형 팬카페 SaaS |
| 모바일 앱 | React Native 앱 개발 |

---

## 9. 결론 및 권장 사항

### 9.1 강점

```
✅ Clean Architecture 기반의 체계적인 설계
✅ Repository 패턴으로 데이터 접근 추상화
✅ TypeScript + Supabase 타입 안전성
✅ Server Actions로 간소화된 서버 통신
✅ 포괄적인 관리자 도구 (25개 페이지)
✅ 보안 고려된 권한 시스템
```

### 9.2 개선 필요 영역

```
⚠️ 순환 참조 제거 필요
⚠️ 테스트 커버리지 확대 필요
⚠️ CRUD 코드 중복 개선
⚠️ 문서화 강화 필요
⚠️ 모니터링 도구 추가 필요
```

### 9.3 즉시 실행 항목 (이번 주)

| # | 항목 | 파일 | 예상 시간 |
|---|------|------|----------|
| 1 | 순환 참조 제거 | `context/index.ts` | 30분 |
| 2 | Utils 경로 통일 | `lib/utils.ts` | 30분 |
| 3 | 미사용 코드 정리 | `lib/utils/mock.ts` | 1시간 |

### 9.4 최종 권장 사항

```
┌─────────────────────────────────────────────────────────────────┐
│                    핵심 권장 사항 요약                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. 코드 품질 우선                                              │
│     → 순환 참조 제거, 중복 코드 정리                            │
│                                                                  │
│  2. 테스트 문화 정착                                            │
│     → 모든 Server Actions에 단위 테스트 추가                    │
│                                                                  │
│  3. 모니터링 체계 구축                                          │
│     → Sentry + Vercel Analytics 통합                            │
│                                                                  │
│  4. 점진적 개선                                                 │
│     → 기능 추가보다 안정화 우선                                 │
│                                                                  │
│  5. 문서화 습관화                                               │
│     → 모든 변경사항 문서 업데이트                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 부록

### A. 데이터베이스 ERD

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   profiles  │────▶│  donations  │◀────│   seasons   │
│             │     │             │     │             │
│ id (PK)     │     │ donor_id    │     │ id (PK)     │
│ nickname    │     │ season_id   │     │ name        │
│ role        │     │ episode_id  │     │ is_active   │
│ unit        │     │ amount      │     └─────────────┘
└─────────────┘     └─────────────┘            │
      │                   │                    │
      │                   ▼                    │
      │           ┌─────────────┐              │
      │           │  episodes   │◀─────────────┘
      │           │             │
      │           │ season_id   │
      │           │ is_rank_    │
      │           │   battle    │
      │           └─────────────┘
      │
      ▼
┌─────────────┐     ┌─────────────┐
│organization │────▶│ live_status │
│             │     │             │
│ profile_id  │     │ channel_id  │
│ is_live     │     │ is_live     │
│ unit        │     │ viewer_count│
└─────────────┘     └─────────────┘
```

### B. 기술 스택 상세

| 카테고리 | 기술 | 버전 |
|----------|------|------|
| Framework | Next.js | 16.1+ |
| UI Library | React | 19 |
| Styling | Tailwind CSS | 4 |
| Component | shadcn/ui | latest |
| Component | Mantine | latest |
| Backend | Supabase | latest |
| ORM | Supabase Client | latest |
| Auth | Supabase Auth | latest |
| Storage | Cloudinary | latest |
| Deploy | Vercel | latest |

---

*보고서 작성: Claude Code*
*최종 업데이트: 2026-01-23*
